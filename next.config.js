/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration pour le déploiement sur Netlify/Vercel
  output: 'standalone',
  // Pas de configuration serveur nécessaire pour frontend-only
}

module.exports = nextConfig

